* Implement proper caching in the service worker so app can be installed as a PWA
* Persist data across page loads
* Update calendar at midnight
* handle recurring payments with multiple types of periods
* * daily
* * weekly
* * bi-weekly
* * monthly
* * semi-annually
* * annually
* * nth weekday of month like "the second Sunday of each month"
* handle automatic payments (show due until the current day is past due date, then automatically set to paid)
* import/export
* i18n - See Intl, it can format currency, dates (and relative time!), etc
